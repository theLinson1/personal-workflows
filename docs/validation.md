# 验证记录

## v0.2（2026-09-22）

实际环境：macOS 26.6.2 / Python 3.9.6。校验与安装脚本只依赖标准库；上游 skill/plugin 校验器需要 PyYAML，本次在独立临时虚拟环境中安装使用，没有修改系统 Python。

实际执行并通过：

- `python3 scripts/validate.py`：2 个清单版本与字段一致，7 个 skill 的元数据、核心组名称、UI 提示与本地 Markdown 引用通过。
- `python3 -m unittest discover -s scripts -v`：13 项通过。验证默认 core、动态 all、按名称选择、参数互斥、dry-run 不写文件、完整 copy/link、重复安装跳过、冲突全量预检、未知名称拒绝、自复制路径拒绝、完整真实包每个资源的字节一致性，以及缺失核心成员能被检测。
- 上游 `skill-creator/scripts/quick_validate.py`：7 个 skills 全部通过。
- 上游 `plugin-creator/scripts/validate_plugin.py`：兼容清单通过。
- 独立 ZIP 解压：45 个源文件逐一比对一致；在解压目录重新运行包校验和全部 13 项安装测试，通过。不依赖源仓库的 `.git` 或本机技能路径。
- `git diff --check`：通过。迁移内容检查未包含源机绝对用户路径、招聘薪资材料或常见凭据标记；这属于内容核查，不宣称能穷尽所有敏感信息形式。

每项检查只支持其覆盖的结论。标准库安装器已在本机实际执行，Windows 是 v0.1 历史验证环境，本次没有重新运行 Windows 端；公司工程、Codex 新任务实际技能发现、iOS 构建、设备联调和真实服务调用仍未验证。

本地源文件没有被修改，实际安装和测试产物只写入临时目录；全局技能目录仅执行 dry-run。

### 独立场景推演

独立代理只接收三个场景与包内 skill 路径，不读取本包的对照、设计或验证文档，再生成实际回复；主代理读取了三份回复后作以下记录。场景是为方法验证构造的请求，不是公司项目事实。

| 给定请求 | 实际回复中的行为 | 未证明的内容 |
|---|---|---|
| 两端账号逻辑迁移 KMP，CMP 未定，无仓库 | 给出业务共享优先的候选路线、不同选项、最小切片及回退；承认没读代码，CMP 未定仅阻塞 UI 实施 | 未实际编译、迁移或证明方案适合公司工程 |
| 两次录音重连修复无效，旧取消回调晚到，无真机 | 停止叠加补丁，提出旧回调污染新状态假设；给单变量事件重放、支持与反证条件；没有宣称根因成立 | 未执行重放、确认根因或修好设备 |
| 30 分钟 KMP 取消传播独立练习，无工程 | 交付入口能力、单一增量、练习、证据、反馈和迁移题；标注教学夹具/伪代码，没有误入等待式教学合同 | 未运行 Kotlin，也没有真实学习者作答 |

三条显式调用后的回复未发现阻断问题。这是有限场景推演，不是自动触发准确率、真实教学效果或真实工作流效果评测。

# v0.1 历史验证记录

版本：0.1.0。日期：2026-09-21。环境：Windows / Python 3.14.5。

实际执行并通过：

- Codex `skill-creator` 的 `quick_validate.py`：5 个 skills。
- Codex `plugin-creator` 的 `validate_plugin.py`：兼容插件清单。
- 本包 `scripts/validate.py`：两个清单的一致性、技能元数据与本地 Markdown 引用。
- 安装器隔离验证：dry-run 不写文件；复制安装包含全部资源；重复安装跳过；只安装所选技能；未知名称失败；同名不同内容拒绝覆盖；冲突发现前不开始安装其他技能；链接安装及再次安装。
- ZIP 独立解压后重新运行校验和复制安装，全部资源可用；ZIP 校验通过，内容与源文件逐项一致。

这些检查证明文件结构和上述安装行为。没有在公司 Mac、目标 iOS 工程、硬件设备或真实工作流中验证；没有据此声称已经识别个人长期习惯、适配公司项目或证明方法效果。

后续用实际任务补充：任务类型、用了哪个方法、发生什么用户纠正、是否改善结果、应保留或收窄哪条规则。内部项目证据保留在项目一侧，个人仓库只记录可独立成立的方法与验证状态。
