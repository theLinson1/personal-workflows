# 明天开始用

本包有 **6 个工程方法 + 1 个可选学习设计**。项目 context 入职后从实际资料补齐；面试时的口述信息不能替代已验证的工程事实。

## 1. 把完整包带到新电脑

从 GitHub 克隆包含 v0.2 的分支，或解压 v0.2 handoff ZIP；不要只复制某个 `SKILL.md`，它可能需要同目录的 references/assets。

在包目录运行（Windows 将 `python3` 换成 `python`）：

```bash
python3 scripts/validate.py
python3 scripts/install.py --profile core --dry-run
python3 scripts/install.py --profile core --mode link
```

无法创建链接时使用 `--mode copy`。默认安装到 `~/.agents/skills`。已有同名不同内容时安装器会停在写入前，先比较，不覆盖；如果还装着本地的同用途 skills，按 [对照表](docs/local-comparison.md) 选择使用入口，避免重复流程。

新开 Codex 任务确认六个技能可见。链接模式保留克隆目录，复制模式更新需要重新比较并安装。想带上完整学习设计：

```bash
python3 scripts/install.py --only layered-learning-design --mode link
```

## 2. 资料到手后，先跑一条最小链路

在**实际工作项目**打开任务，给 Codex 当前要完成的小目标，然后使用：

```text
$bootstrap-project-context 我刚接手这个项目。今天先确认：
1. 这个任务对应的模块和代码入口；
2. 按团队约定最小构建/检查怎么运行；
3. 一个相关用户动作经过哪些已能确认的模块或服务。
先读已有文档和相关代码，已有答案不重复问；不要假设硬件协议或架构。
记录来源、验证状态、当前阻塞项和下一步。文档留在当前工作项目。
```

没有仓库、工具或设备时，记录缺口和获取方式，不为了写满文档假装已接通。业务共享与 UI 共享分开确认；外部服务能力从实际接口契约确认。

## 3. 碰到问题时选入口

| 当前情况 | 可以直接说 |
|---|---|
| 目标不清楚 | `$frame-problem 先分清已确认目标、关键未知和它们阻塞哪一步。` |
| 有方案取舍 | `$compare-options 比较可行路线，给推荐、证据、代价和什么会改变推荐。` |
| 想深挖设计 | `$compare-options 按方案访谈模式，一次一问压测这个设计，先查已有答案。` |
| 故障原因不明 | `$debug-with-evidence 先提出可证伪原因和最小诊断，再完成已授权修复。` |
| 准备交付 | `$verify-delivery 根据最后修改后的结果核验，区分静态判断和实际运行。` |
| 今天有经验值得留 | `$capture-workflow 更新可复用方法；项目事实留在项目内，不把候选当成我的长期偏好。` |
| 想学会新机制 | `$layered-learning-design 设计一个有练习、反馈和迁移证据的学习单元。` |

这是选择表，不是每天必须执行的七步。明确的小修改直接完成。

## 4. 下班前留下可接续的状态

只记录今天得到证据的内容：已完成的产物、实际检查结果、未解决项及下一步。复用项目既有任务记录或 context，不另建重复档案。

需要提炼到个人 skill 时，抽象成“触发条件—判断—动作—验证—不适用情形”，去掉内部事实；新规则说明替代或收窄了什么。没有足够案例时保留候选状态。

## 已验证与待验证

本次验证覆盖包结构、Mac 上临时目录安装、重复/冲突行为和方法场景推演，详见 [验证记录](docs/validation.md)。公司工程构建、真实设备联调、服务权限与实际工作效果仍需在入职环境验证。
